# G5 · GATE — **CONGELADO**

Estado: FROZEN antes de implementación de producto.
Rama: `g5-editorial-redesign`
Base: `6fc410e3989a07d54a085fe00812c281c92fdcb9` (g4-product-cut;
producto H2 `ec9cf8e`, candidato congelado previo
`4a3d1ed361013d558acb4c3334560737113d9528`).

## Contexto de adjudicación

- G1/G2/G3 semántica y funcionalidad: preservadas.
- G4 automatización: PASS sobre el árbol previo.
- GH5 (revisión visual humana G4): **CHANGES_REQUIRED_BY_HUMAN** —
  ver `docs/g5/HUMAN-FEEDBACK.md`. La aceptación previa parcial de
  orquestador no satisface el gate humano; el humano real revisó y
  pidió rediseño editorial.
- PERF4/GD12: **BLOCKED — adjudicado en R3** (reglas declaradas:
  baseline >3500 → BLOCKED). R1: ambos ~3530 ms, inválida. R2:
  baseline `0563d60` p75=3449 PASS; candidato `67c2618` p75=3539 FAIL —
  regresión real ~+90 ms por grafo JS eager (chunk 62→96 KB).
  Fix `1765a64`: code splitting real del below-fold (`LazyView` +
  `BelowFold`, chunk de página 96→80 KB; `t_headline` ~1590→~1430 ms,
  mejor que el baseline). R3 (única repetición pre-declarada,
  `evidence/g5/perf4/R3-PROTOCOL.md`): baseline p75=3588 FAIL;
  candidato `1765a64` p75=3576 FAIL — **−12 ms bajo el baseline**.
  El umbral absoluto no es adjudicable de forma estable en esta máquina
  (el mismo baseline mide 3449–3588 ms entre sesiones); sin regresión
  en p75 vs baseline en la misma sesión. Observación no bloqueante:
  p95 candidato 3758 vs 3647 baseline (+111 ms; sin conclusiones con
  n=20). Umbrales congelados, no movidos. Evidencia completa:
  `evidence/g5/perf4/COMPARISON.md`.
- NVDA real / móvil físico: PENDING_HUMAN hasta prueba real.

## Filosofía congelada

Pieza de periodismo de datos seria + mapa explorable + investigación con
datos oficiales. NO dashboard SaaS, NO grid de tarjetas, NO panel GIS,
NO informe de metodología con mapa adjunto. Secuencia emocional:

RESPUESTA → DÓNDE → CUÁNDO → COMPRUÉBALO CON IMÁGENES →
QUÉ MÁS SABEMOS DEL LUGAR → CASOS QUE MERECE LA PENA MIRAR →
TU CALLE → CÓMO LO SABEMOS

La complejidad vive debajo; la experiencia visible es simple.

## Criterios (binarios)

### Semántica (no negociable)

- GS1: nada en el producto afirma reconstrucción del parque histórico.
  El tiempo = ordenar los edificios actuales por su año registrado.
- GS2: C-05 = edificios actuales con año VÁLIDO > Y / con año VÁLIDO.
  La validez geométrica NO entra en C-05; ningún copy público dice lo
  contrario.
- GS3: C-08 = huella de edificios con año válido y geometría aceptada/
  reparada (C-06). C-05 y C-08 nunca se confunden.
- GS4: ortofotos = evidencia visual; nunca fuente de métricas. Mapa
  1923–25 = cartografía, nunca foto ni reconstrucción catastral.
  Planeamiento = capacidad registrada, nunca predicción. Contexto =
  solape/proximidad/clasificación actual, nunca causalidad.
- GS5: `UNKNOWN ≠ 0` en todo dato contextual (ningún valor ausente
  renderiza como 0 ni se interpola como observación).

### Modelo temporal

- GT1: un solo eje temporal catastral. Sin marcas de campaña de
  ortofoto ni de 1923–25 dentro de ese eje (aserción DOM).
- GT2: «1923–25» se presenta como mapa histórico standalone, con copy
  que dice explícitamente «es un mapa, no una fotografía».
- GT3: los modos se agrupan conceptualmente LEER EL DATO (Edificios,
  Tiempo) vs COMPROBAR CON OTRAS FUENTES (Foto aérea, Mapa histórico);
  nunca cuatro botones equivalentes.

### Visual anti-AI

- GV1: ≤2 contenedores de contenido con border-radius en RESULT fuera
  de formularios/controles/leyenda de mapa (medido por sonda DOM).
- GV2: overview del mapa sin bordes de rejilla visibles; edificios
  protagonistas a zoom alto (verificación por captura + capas).
- GV3: comparación de foto por defecto SIN solape de opacidad ni swipe:
  dos paneles sincronizados (desktop) o toggle segmentado (móvil).
- GV4: sin overlay de edificios actuales sobre imagen histórica por
  defecto; si se activa, solo contorno y etiqueta «Edificios del
  Catastro actual».
- GV5: paleta reforzada (tokens `docs/g5/VISUAL-SYSTEM.md`), contraste
  WCAG verificado, hatch para «sin año utilizable».

### Comprensión / copy

- GC1: primer viewport 390×844 muestra contexto lugar+año, cifra
  principal, frase en lenguaje llano e inicio del mapa; ≤5 controles
  explícitos visibles.
- GC2: la cifra principal es comprensible sin metodología; el disclosure
  público muestra la cuenta en lenguaje llano (n÷m = %) sin IDs
  contractuales.
- GC3: el copy público por defecto no muestra C-04/C-05/C-06/C-08,
  `DATA_SEMANTICS`, `Ano_Constr` ni jerga numerador/denominador
  (aserción copylint).
- GC4: «huella en planta» se traduce la primera vez («el suelo que
  ocupa la geometría del edificio») y se declara que no es superficie
  construida total.
- GC5: aproximación humana («casi 6 de cada 10») solo si es
  determinista, con el porcentaje exacto visible; umbrales de borde
  probados (x.45/x.55).

### Contexto y fuentes

- GF1: máx. 2–3 hechos genéricos en «Qué más sabemos del lugar»; cada
  uno con fuente y fecha; línea editorial, no tarjeta KPI.
- GF2: cualquier fuente nueva pasa el gate de adopción (oficial,
  reproducible, licencia clara, pregunta explícita, semántica segura,
  cobertura adecuada, valor material). Como máximo 1 dataset genérico
  municipal nuevo + contexto story-specific ya evidenciado.
- GF3: Open Data Bizkaia sigue siendo la familia principal; fuentes
  complementarias citadas con fuente+fecha junto al dato.

### Historias

- GH1: índice editorial de 5 líneas, no grid de tarjetas; un capítulo
  destacado.
- GH2: copy de cada historia no excede su brief congelado; contexto
  actual etiquetado como actual con denominador de solape; sin
  causalidad ni reconstrucción histórica.
- GH3: jerarquía de acciones preservada: primaria dinámica
  (Ver en el tiempo/mapa), secundaria (Míralo desde el aire),
  terciarias (Otro, Volver); 44px; teclado; foco editorial.
- GH4: restauración de estado personal intacta («Volver a mi Bizkaia»).

### Accesibilidad

- GA1: axe 0 en todos los estados; orden de lectura = orden visual;
  controles agrupados etiquetados; alternativas textuales para foto,
  mapa histórico y gráfico de décadas (tabla accesible).
- GA2: teclado completo, foco visible, 44px, 320px, zoom 400%,
  reduced-motion, hatch como segundo canal de color.
- GA3: NVDA real y móvil físico quedan PENDING_HUMAN.

### Rendimiento / arquitectura

- GP1: contrato de critical path ampliado — 0 peticiones L3/L4 (foto,
  histórico, stories, NORA, planning, contexto, dataset contextual,
  índice de edificios, comparador) antes de result readiness.
- GP2: nada de trabajo nuevo en la ruta crítica inicial; si el rediseño
  reduce JS inicial, se documenta (informe estático
  0563d60 vs 4a3d1ed vs candidato G5).
- GP3: hooks del harness congelado preservados: `.headline-block h1`,
  `.lead2`, `.mapband canvas` — medición del mismo hito de producto.
- GP4: dependencias muertas eliminadas (maplibre-gl-swipe si el swipe
  desaparece, componentes/CSS/card legacy); lockfile y tests migrados
  honestamente.

### No regresión

- GN1: batería completa G1/G2/G3/G4 funcional PASS con probes migrados
  donde cambiaron selectores; sin debilitar aserciones semánticas.
- GN2: deep links heredados (`year/place/lat/lon/z/view/play/ortho/
ortho2/building/compare/story`) siguen funcionando o traducidos.
- GN3: check/lint/format/unit/build PASS; matriz 320/390/768/1440,
  Chromium/Firefox/WebKit, axe, reduced-motion, teclado, errores de
  red/imagen, back-forward, reload, orientación.

### Revisión humana

- GRH1: pack humano G5 regenerado (`evidence/g5/human-review/`:
  contact-sheet + HOME 1440/390 + RESULT estados + 5 historias ×2
  anchos + fallos). Checklist explícito (¿sigue pareciendo generado?
  ¿demasiadas cajas? ¿colores suficientes? ¿modelo temporal claro?
  ¿1923–25 lee como mapa? ¿fotos separadas? ¿porcentaje comprensible?
  ¿más que edificios? ¿memorable? ¿un juez lo explica en 30 s?).
- GRH2: `GH5-G5 = PENDING_HUMAN` hasta aceptación humana real.
  Ningún agente se auto-acepta.

## Estado de ejecución (2026-09-20, candidato `g5-editorial-redesign`)

Implementación completa y verificada. **GH5-G5 = ACCEPTED (humano)** —
veredicto tras spot-check de la ronda 2 sobre el preview: «las tres
reservas principales quedan cerradas… está en estado presentable para
jurado». PERF4 adjudicado tras R3 controlada pre-declarada
(`evidence/g5/perf4/R3-PROTOCOL.md`, commit `b160548`): **BLOCKED** —
el baseline falló el absoluto en la sesión R3 (3588 ms); el candidato
`1765a64` (post code-splitting) quedó **12 ms por debajo** del baseline
(3576 ms), sin evidencia de regresión. Historial completo R1→R3 y
diagnóstico en `evidence/g5/perf4/COMPARISON.md`.

Detalle menor anotado por el humano (NO bloqueante): en pantallas muy
anchas la vista inicial deja bastante aire vacío a la derecha del
titular.

### Revisión humana ronda 2 — spot-check (2026-09-20)

- Eje temporal: PASS — desde el pie, «En el tiempo» deja el eje
  inmediatamente visible y centrado; lectura TU AÑO / REPRODUCCIÓN ok.
- Dato humano: PASS — población con regla roja añade escala territorial
  sin competir con el titular.
- Porcentaje: PASS — Leioa 2010 («algo más de 1 de cada 10…») y Abanto
  («menos de 1 de cada 10») comprensibles sin metodología.
- Rampa: mejora clara — el mapa ya no se pierde contra el papel a vista
  amplia; presencia sin volverse dashboard.
- Jerarquía del resultado validada: titular → traducción humana → dato
  local → cobertura → mapa.
- El humano decide NO reabrir las etiquetas estructurales de las
  historias ni el título editorial.

### Revisión humana ronda 1 (dictamen del humano, 2026-09-20)

Veredicto: «la mejora es real — ya parece más una pieza de periodismo de
datos que una interfaz generada. No añadiría más cajas ni más colores.»
Tres prioridades antes de enseñarlo a un jurado, todas implementadas en
la ronda 2 (commit posterior):

1. **Eje visible al entrar en «En el tiempo»**: el eje se insertaba sobre
   el mapa y el scroll podía dejarlo fuera de pantalla → ahora
   `scrollIntoView` en el cambio de modo por el usuario (suave; instantáneo
   con reduced-motion; nunca en deep link). Verificado: `.timeband`
   queda centrado tras el click desde el pie de página.
2. **Un dato humano junto al resultado**: población del padrón Eustat
   bajo el lead («Leioa tiene hoy 32.495 habitantes empadronados (Eustat,
   padrón de 2025)»). Inyectada dentro de `metrics/<slug>.json` por
   `pipeline/g5_population_into_metrics.py` (112/112, QA
   `g5_metrics_population.json`) — cero peticiones nuevas: GP1 intacto.
3. **Porcentaje más explícito + cobertura más simple**: nueva línea
   `result.plain.*` — «Es decir: casi 5 de cada 10 edificios actuales de
   Leioa son más jóvenes que tú» — con bordes gramaticales
   (`approxKind`, tests en `human.test.ts`); cobertura reducida a una
   frase. Verificado también con resultado bajo (2010 → 13 %: «algo más
   de 1 de cada 10… son más jóvenes que tú»).

Secundario aplicado: rampa de celdas con extremo bajo más oscuro
(`#ead9c0` en vez de casi-papel) — petición del humano, sin colores
nuevos. Observación sin cambio deliberado: la repetición
«Qué vemos / El dato / Qué sabemos…» es el formato interno de cada
capítulo de historia (etiquetas estructurales), no una cuadrícula de
tarjetas; se deja como está para no reabrir GH1–GH3.

Regresión tras ronda 2: g2a PASS (20/20), g2b PASS (incl. axe en todos
los estados), g4_atlas PASS (a1vp=5), perf4 critical path PASS
(0 violaciones). Capturas regeneradas en `evidence/g5/shots/` +
`evidence/g5/r2-*.png`.

| Criterio                    | Estado            | Evidencia                                                                                                       |
| --------------------------- | ----------------- | --------------------------------------------------------------------------------------------------------------- |
| GS1–GS5 semántica           | PASS              | copylint 124 tests; `UNKNOWN` nunca renderiza 0 (g3b); copy público sin C-xx/`Ano_Constr`/`DATA_SEMANTICS`      |
| GT1 eje catastral único     | PASS              | `f1_axis_cadastral_only`: `.timeband .camp` = 0 (g2a)                                                           |
| GT2 histórico standalone    | PASS              | copy «es un mapa, no una fotografía»; opt-in 0→29 req (g3c)                                                     |
| GT3 modos agrupados         | PASS              | «Leer el dato» / «Comprobar con otras fuentes» (ViewSwitch)                                                     |
| GV1 anti-AI                 | PASS              | ≤2 contenedores con radius en RESULT (tokens sin cards)                                                         |
| GV2 sin rejilla             | PASS              | capturas `evidence/g5/shots/d-*.png`                                                                            |
| GV3 sin swipe               | PASS              | dúo sincronizado w1440 / toggle w390 (`mode-photo-*` del pack)                                                  |
| GV4 sin overlay por defecto | PASS              | overlay opt-in, solo contorno etiquetado                                                                        |
| GV5 paleta + contraste      | PASS              | `ink3` #655f54 → ≥5.2:1; axe 0 violaciones todos los estados                                                    |
| GC1 primer viewport         | PASS              | 5 acciones a 390px, presupuesto ≤6 (g4_atlas `a1vp`)                                                            |
| GC2–GC5 copy llano          | PASS              | disclosure n÷m sin IDs; aproximación humana con tests de umbral                                                 |
| GF1–GF3 contexto            | PASS              | 2 hechos con fuente+fecha; Eustat pasa gate de adopción (CONTEXT-SOURCES.md); Open Data Bizkaia sigue principal |
| GH1–GH4 historias           | PASS              | índice editorial 5 líneas; `story-*` del pack; restauración intacta (g4_state_matrix)                           |
| GA1–GA2 accesibilidad       | PASS              | axe 0 en todos los estados; 44px; reduced-motion; teclado                                                       |
| GA3 NVDA/móvil físico       | **PENDING_HUMAN** | casos congelados: `evidence/g5/nvda/CASES.md` + `evidence/g5/mobile/CASES.md` (+ árbol a11y `nvda/a11y-tree/`)  |
| GP1 critical path           | PASS              | 0 peticiones L3/L4 (incl. `eustat-population`) antes de readiness (perf4)                                       |
| GP3 hooks harness           | PASS              | `.headline-block h1`, `.lead2`, `.mapband canvas` intactos                                                      |
| GP4 deps muertas            | PASS              | `maplibre-gl-swipe` eliminado; `PlanningContext.svelte` borrado                                                 |
| GN1 regresión               | PASS              | g2a/g2b/g3b/g3c/g4_atlas/g4_scene_stories/g4_state_matrix PASS                                                  |
| GN2 deep links              | PASS              | `year/place/view/ortho/ortho2/play/compare/building/story` verificados                                          |
| GN3 matriz                  | PASS              | Chromium/Firefox/WebKit smoke PASS; 320/390/1440; reduced-motion                                                |
| GRH1 pack humano            | DONE              | `evidence/g5/human-review/` (25 capturas + contact-sheet)                                                       |
| GRH2 aceptación humana      | **PASS (humano)** | dictamen ronda 1 + spot-check ronda 2 — arriba                                                                  |
| PERF4 cronometrado          | **BLOCKED (R3)**  | baseline >3500 en R3; candidato `1765a64` −12 ms vs baseline, sin regresión — ver `evidence/g5/perf4/`          |

## Estados terminales

- `G5_PASS`: todos los criterios automatizables + PERF4 calibrado PASS
  sobre el candidato congelado G5 + aceptación visual humana real.
- `G5_FAIL`: cualquier criterio falla, o baseline PASS + candidato FAIL
  en PERF4.
- `G5_BLOCKED`: baseline calibrado `0563d60` falla → adjudicación de
  rendimiento imposible.

## Fuera del gate

- PERF4: cerrado tras R3 con adjudicación BLOCKED (umbral absoluto no
  validable en el entorno; sin regresión del candidato). Pendiente solo
  de adjudicación humana final si se requiere PASS explícito.
- G6 (post-gate): modo `swipe` «1956 / hoy» añadido por petición explícita
  (ADR-016). No invalida GV3 — que evalúa la comparación de campañas de
  FOTO (dúo/toggle, sin cortina); el swipe es un modo propio con ámbito
  fijo primera↔última campaña, cubierto por `g5_swipe.mjs` y los contratos
  lazy/critical-path. Casos NV-18/19 y MOB-05b añadidos a las tablas GA3.
- Integración de corpus legislativo (legalize/leyabierta): catalogado,
  sin integración.
- Despliegue público final (gh-pages): solo tras aceptación humana +
  PERF4 + decisión humana de requisitos NVDA/móvil.
