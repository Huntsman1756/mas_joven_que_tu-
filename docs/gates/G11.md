# G11 — RENOVACIÓN VISUAL / atlas personal de Bizkaia

> Input normativo: dirección de diseño del auditor (conversación post-G10.1).
> Base: `630d581` + docs `a8b9872` (G10.1 cerrado, cero defectos técnicos
> conocidos). Rama: `g11-visual-renewal`.
>
> Decisión del usuario: **rediseño completo en una pasada**; pruebas humanas
> `NV-18/19` + `MOB-05b` **diferidas al candidato G11**; consolidación de
> navegación a 3 superficies **pendiente de decisión tras el corte vertical**
> (de momento se conservan los 5 modos y ADR-019).

## Identidad objetivo

«Atlas personal de Bizkaia»: territorio reconocible + lectura editorial +
interacción precisa. La inversión visual va al territorio y su comparación.

## Alcance (del brief)

| # | Superficie | Cambio |
|---|------------|--------|
| G11-01 | Portada | composición 40/60, copy corto («Tu municipio también tiene edad»), formulario con labels reales, imagen = recorte real de un lugar concreto con cobertura verificada 1956+2025 |
| G11-02 | Tipografía | Newsreader (titulares narrativos) + Source Sans 3 (UI, cifras, tablas); self-hosted OFL; escala del brief |
| G11-03 | Paleta | fondo #F7F8FA, paneles #FFF, ink #182631/#52606D, líneas #DCE2E7, acción #A8372A, después #C94F38, antes #52768E, noyear trama; conservar opacidad+trama de G10-13 |
| G11-04 | Resultado | panel narrativo 340–400 px + mapa misma vista (65–75 vh); sin fila KPI en cabecera; población/década a sus capítulos |
| G11-05 | Mapa chrome | zoom+recenter agrupados, leyenda compacta (bajo mapa en móvil), selección con contorno, mensaje de escala, referencias territoriales si la fuente lo permite |
| G11-06 | Fotos/comparación | pareja de campañas relevante primero (cercana al nacimiento + última), «Elegir otras fechas» después; móvil `[año][año][Comparar]` |
| G11-07 | Historias | entrada por imagen real + lugar identificado + hallazgo + «Explorar este lugar»; ficha técnica en detalle |
| G11-08 | Histograma móvil | barras horizontales por periodo + frase interpretativa + acceso a tabla/metodología |
| G11-09 | Iconos | Lucide conservado; sin iconos ornamentales en KPI retirados; 20/24 px, trazo 1,75–2 |

## Invariantes heredados (no negociables)

- Contratos de datos, métricas y semántica de G1–G10 intactos:
  `metrics.frozen.test.ts`, `bucketRenderState`, `parseYearInput`,
  `null≠0`, cobertura/denominador explícito en primera lectura.
- Reduced-motion, teclado, touch y presets de cortina (G10.1) intactos.
- Contraste AA recomputado con la paleta nueva antes de cerrar.
- Sin fuentes nuevas de datos; las imágenes de portada/historias se
  generan desde las ortofotos ya catalogadas, con cobertura verificada.
- Hooks de test (clases/atributos usados por g8/g10) se conservan o se
  actualizan los scripts — nunca se rompe la suite para que «pase».

## Verificación de salida

- `check`/`lint`/`format`/`test` verdes + suites E2E (`g8`, `g2b`,
  `g5_swipe`, `g10_hardening`) adaptadas donde el DOM cambió.
- axe en todos los modos + PERF4 lazy/critical-path.
- Review visual: 320/360/390/768/1024/1440/1920, home + resultado +
  comparación + histograma, móvil y desktop.
- Evidencia en `evidence/g11/`.

## Estado (corte vertical implementado)

| # | Estado | Nota |
|---|--------|------|
| G11-01 portada | DONE | 40/60, `Tu municipio también tiene edad.`, labels reales, díptico real Abandoibarra 1956/2025 (`data/hero/` + manifest), orden móvil corregido (formulario antes que imagen) |
| G11-02 tipografía | DONE | Newsreader 500 (relato) + Source Sans 3 400/600/700 (UI) self-hosted OFL, `static/fonts/` + `fonts/licenses/` |
| G11-03 paleta | DONE | tokens nuevos en `lib/palette.ts` + `:root`; toda la chrome migra a `var(--*)` (156 reemplazos); en `MapView` los paint de MapLibre usan los hex nuevos (`var()` no resuelve ahí); contrastes `evidence/g11/contrast.json` |
| G11-04 resultado | DONE | panel narrativo ~340–400 px + mapa misma vista; fila KPI retirada (`facts.*` huérfanas); `view.cta_era` enlaza a la campaña cercana |
| G11-05 mapa chrome | DONE | leyenda compacta con extremos `0 %`/`100 %`, mensaje de escala «Vista por zonas…», selección con contorno tinta |
| G11-06 fotos | PARTIAL | swipe «antes» = `app.nearest` (pareja relevante primero); `PhotoPanel` ya abre en la campaña cercana. «Elegir otras fechas» y presets `[año][año][Comparar]` móvil = backlog |
| G11-07 historias | DONE | image-led con thumbs oficiales + contexto + «Explorar este lugar →» |
| G11-08 histograma móvil | DONE | barras horizontales + frase interpretativa; «sin año» neutro/trama |
| G11-09 iconos | DONE | Lucide conservado; ornamentales retirados con la fila KPI |

**Tests adaptados (no relajados):** `g10_hardening` — selectores de
hotspots acotados a `.hot` (el nuevo CTA del panel ampliaba el match);
`g5_swipe` — `req_1956` → `req_before` (ORTO_1989, la campaña más cercana
a 1987). Batería: `check` 0 err (2 warnings preexistentes
`state_referenced_locally`), `lint`/`format` limpio, `test` 181/181,
`g10_hardening` 31/31, `g8_viewer` 28/28, `g2b_views` 30/30,
`g5_swipe` pass.

## G11.1 — pasada de refinamiento (review visual del auditor)

Seis hallazgos del review sobre `b990bb3`, todos resueltos:

| Hallazgo | Resolución |
|----------|------------|
| Campo «Municipio» rebasaba el margen en móvil; campos montados en desktop | `box-sizing: border-box` global en `+page.svelte` |
| Mapa inicial «vacío» | **Bug real**: `featureShare()` cacheaba `null` mientras la serie de celda seguía cargando y lo dejaba fijado. Corregido: no se cachea `null` con serie ausente. Sonda post-fix: 458/459 celdas con `share` real (antes 0/459) |
| Resultado móvil ocupaba ~690 px antes del mapa | Panel compactado ≤700 px (menos padding, `.scope`/`.plain` ocultas — el universo sigue en el titular —, CTA reducido); mapa entra en la primera pantalla (~490 px) |
| Titular serif grande en columna estrecha | `.post` reducido a `clamp(1.375rem, 1.9vw, 1.625rem)`; el porcentaje sigue dominando |
| Atribuciones superpuestas en comparador | `.src` de la cortina bajo el chip «Actualidad» (`top: 7.4rem` derecha): sin colisión con la atribución MapLibre ni con el control de zoom (ver `evidence/g11/shots/swipe-desktop.png`) |
| Leyenda móvil como overlay absoluto | `.legend` fuera de `.mapwrap`, dentro de `.mapouter` (flex); desktop sigue absoluta, móvil en flujo bajo el lienzo con borde superior. `applyEvidenceVisibility()` reprograma `map.resize()` |

Adicional: limpieza de `var(--ink-3, var(--ink-3))` (fallback duplicado
introducido por la migración masiva de tokens) en `LazyView`/`PlaceSearch`.

Batería re-ejecutada tras los cambios: `check` 0 err (mismos 2 warnings),
`lint`/`format` limpio, `test` 181/181, `g10_hardening` 31/31,
`g8_viewer` 28/28, `g5_swipe` pass. Capturas `evidence/g11/shots/`
regeneradas + `map-probe.png`.

**Pendiente de este gate:**
- NV-18/19 + MOB-05b → sobre el candidato G11.1 **`8824c3d`** (rama
  `g11-visual-renewal`); ya **no** sobre
  `630d581` (G10.1, histórico) ni `b990bb3` (G11, pre-refinamiento).
- Decisión diferida: consolidar navegación a 3 superficies (rompe
  ADR-019; requiere ADR nuevo).
- Backlog documentado: «Elegir otras fechas» y presets
  `[año][año][Comparar]` en móvil (G11-06 PARTIAL).
